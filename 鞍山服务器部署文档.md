---
title: 鞍山服务器部署文档
---
# 问题
1、挂载步骤-挂载不上
2、时间同步不了
3、



### **问题描述**：导入时报错
![Uploading file...sny8t]()
![[12dfa02459e2e07b0ce2cfb43fb09b32.jpeg]]
### **分析过程**：
导入数据过大
### **根本原因**：
### **解决方案**：
mysql配置文件：
	添加:max_allowed_packet = 64M
查看配置：
	show variables like 'max_allowed_packet'
重启mysql



### **问题描述**：双主主键冲突
### **分析过程**：
### **根本原因**：
### **解决方案**：

#### 1、重新同步数据 停从库 + 锁主库 + 全量重同步（中小库、业务可短暂停写）
1. 主库加只读锁，禁止新写入
```sql
-- 主库执行
flush tables with read lock;
```
2. 主库全量备份（mysqldump）
```bash
mysqldump -uroot -p --all-databases --master-data=2 --single-transaction > master_full.sql
```
3. 主库解锁
```sql
unlock tables;
```
4. 从库停复制、清空旧数据、导入备份
```sql
stop slave;
reset slave all;
```

```bash
mysql -uroot -p < master_full.sql
```
5. 从库重新配置主从、启动复制
```sql
CHANGE MASTER TO 
MASTER_HOST='主库IP',
MASTER_USER='repl',
MASTER_PASSWORD='密码',
MASTER_LOG_FILE='对应binlog文件',
MASTER_LOG_POS=对应位点;

start slave;
show slave status\G
```

2、自增偏移错开（最常用）
**主 A**
```ini
auto_increment_increment = 2
auto_increment_offset = 1
```
生成 ID：1,3,5,7…

**主 B**
```ini
auto_increment_increment = 2
auto_increment_offset = 2
```