---
title: 获取当前地址
author: BigSea
email: 2834637197@qq.com
createDate: 2025-09-28 17:38:42
updateDate: 2025-09-28 17:38:42
week: 第39周｜星期日
Country: China
City: NanJing
Weather: 🌧️
uvIndex(1-15): 2
Temperature(℃): 30
CurrentWeatherTime: 3:00 PM
GetWeatherTime: 2025-09-28 17:38:50
Feels Like(℃): 34
Pressure(hPa): 1011
Humidity(%): 68
WindSpeed: 3
WindSpeedDesc: 微风
TempRange(℃): 24-30
SunHour: 9.5h
Sunrise: 05:57 AM
Sunset: 05:54 PM
tags:
---
# 经历：

# api
腾讯api：ip精度过低只能到城市，硬件设备位置接口不太好实现

## js
<font color="#00b0f0">在浏览器可以实现，但是页面无法实现，因为Obsidian不支持定位</font>
```title="js"

if (navigator.geolocation) {
  navigator.geolocation.getCurrentPosition(
    (position) => {
      // 成功获取位置的回调函数
      const coords = position.coords;
      console.log('定位成功：');
      console.log('  纬度 (Latitude):', coords.latitude);
      console.log('  经度 (Longitude):', coords.longitude);
      console.log('  精度 (Accuracy):', coords.accuracy, '米'); // 位置精度信息
      // 其他可能可用的属性，取决于设备支持情况
      // console.log('  海拔 (Altitude):', coords.altitude);
      // console.log('  速度 (Speed):', coords.speed);
    },
    (error) => {
      // 获取位置失败的回调函数
      console.error('定位失败，错误代码:', error.code, '-', error.message);
    },
    {
      // 可选的配置参数
      enableHighAccuracy: true, // 是否尝试高精度定位
      timeout: 10000, // 请求超时时间（毫秒）
      maximumAge: 0 // 允许使用缓存位置的最大年龄（毫秒），0表示不使用缓存
    }
  );
} else {
  console.error("抱歉，您的浏览器不支持地理定位功能。");
}
```

