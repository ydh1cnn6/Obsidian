指定泛型，在getclass时实际并没有泛型，比如list<T>,实际上无论里面是什么，getclass时都相等，而不是list<String>之类的具体类型，泛型类或者泛型参数，会被替换成第一个上界，没有边界时就是object(比如指定了上界Number那就是Number)

泛型不支持基础类型，因为泛型会object。

通过反射注入，可以绕过指定的类型，比如list<string>中可以塞入int类型而不报错
list.getClass().getMethod( name:"add", Object.class).invoke(list, args: 233);