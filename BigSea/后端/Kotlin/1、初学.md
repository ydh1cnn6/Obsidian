---
title: 1、初学
author: BigSea
email: 2834637197@qq.com
createDate: 2025-09-03 17:38:17
updateDate: 2026-06-30 16:53:25
week: 第36周｜星期三
Country: China
City: NanJing
Weather: ☁️
Temperature(℃): 33
Feels Like(℃): 37
Pressure(hPa): 1006
Humidity(%): 53
WindSpeed: 1
WindSpeedDesc: 软风
TempRange(℃): 23-33
uvIndex(1-15): 8
SunHour: 11.0h
Sunrise: 05:42 AM
Sunset: 06:27 PM
---
Hongkou, China: 🌦   🌡️+28°C 🌬️↖11km/h
[Kotlin快速入门 —— 基础知识_kotlin基础-CSDN博客](https://blog.csdn.net/weixin_45922212/article/details/115557579)


# 基本语法

# 逻辑控制
## 循环
**区间：** Kotlin有区间的这个概念，示例如下:
- 两端闭区间（[0,10]）：`val range 0..10`
- 左闭右开区间（[0,10)）：`val range = 0 util 10`
- 降序区间（[10,1]）：`var range 10 downTo 1`
**步长：** step关键字，使用示例：
```kotlin
for (i in 0..10 step 2) {
	//0 2 4 6 8 10
	println(i)
}
```

# 面向对象
定义类

```kotlin
class Person {
	var name = ""
	var age = 0
	
	fun eat() {
		println("eating!!!")
	}
}
```

创建对象
```kotlin
val p = Person()
```


## 继承与构造函数
在Kotlin中任何一个非抽象类默认都是不可以被继承的，相当于Java中给类声明了final关键字。
加上open关键字后，可以被继承。继承自定义类， Kotlin不是用extends，而是变成一个冒号
```kotlin
//Person类后面要加上括号，这里和Java不一样。这里涉及到主构造函数和次构造函数相关内容
class Student : Person() {
	var sno = ""
	var grade = 0
}
```

主构造函数：

每一个类都有一个默认的不带参数的主构造函数。主构造函数的特点是没有函数体，直接定义在类名的后面。示例：
```kotlin
class Student(val sno: String, val grade: Int) : Person() {
}
```
这里将sno和grade字段放入主构造函数当中，表名在对Student类进行实例化时，要传入构造函数中要求的所有参数，示例：
```kotlin
val Student = Student("a123", 5)
```
当需要在主构造函数中编写一些逻辑时，可以利用init结构体，示例：
```kotlin
class Student(val sno: String, val grade: Int) : Person() {
	init {
		println(sno)
		println(grade)
	}
}
```
Kotlin中也规定：子类中的构造函数必须调用父类中的构造函数。但是主构造函数并没有函数体，所以Kotlin通过括号来实现。  
上诉代码中，Person类后面的一对空括号表示Student类的主构造函数在初始化的时候会调用Person类的无参构造函数。





# 语法糖
|语法|对应函数|说明|
|---|---|---|
|`a[key]`|`a.get(key)`|下标读取|
|`a[key] = value`|`a.set(key, value)`|下标写入|
|`a()`|`a.invoke()`|函数式调用|
|`a + b`|`a.plus(b)`|加法|
|`a in b`|`b.contains(a)`|包含判断|
